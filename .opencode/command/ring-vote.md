---
description: Ask all three rotation models the same question in parallel. Tally the answers.
---

Run the following question through all configured rotation models **in parallel**, each as themselves (not under a specific role — just the model's honest take). Then report:

1. Each model's answer, verbatim (1-3 sentences each).
2. The majority position, if one exists (2-of-3 agreement or 3-of-3).
3. Any dissenting opinion, preserved as-is — do not average or soften.
4. A one-line recommendation on whether the majority should win, or whether the dissent surfaces something the majority missed.

Do **not** edit any files. This is decision support, not an action.

---

Question: $ARGUMENTS
