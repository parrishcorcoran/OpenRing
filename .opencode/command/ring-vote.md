---
description: Ask Architect, Adversary, and Grinder the same question in parallel. Tally the answers.
---

Run the following question through all three ring subagents **in parallel**, then report:

1. Each subagent's answer, verbatim (1-3 sentences each).
2. The majority position, if one exists (2-of-3 or 3-of-3 agreement).
3. Any dissenting opinion, preserved as-is — do not average or soften.
4. A one-line recommendation on whether the majority should win, or whether the dissent surfaces something the majority missed.

Do **not** edit any files. This is a decision-support command, not an action.

---

Subagents to invoke: `@architect`, `@adversary`, `@grinder`.

Question: $ARGUMENTS
